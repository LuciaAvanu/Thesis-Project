<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous"> -->
    <title>Showing a file</title>
</head>

<body>

    <h1>You are viewing
        <%=foundFile.title%>
    </h1>
    <% console.log(foundFile) %>

        <%function spliceSlice(str, index, count, add) {
        if (index < 0) {
          index = str.length + index;
          if (index < 0) {
            index = 0;
          }
        }
      
        return str.slice(0, index) + (add || "") + str.slice(index + count);
    }%>

            <a href="<%=foundFile.data.url%>">View File </a>
            <a href="<%=spliceSlice(foundFile.data.url, 49, 0, '/fl_attachment')%>" download="<%=foundFile.title%>" target="_blank">Download</a>


            <%if(currentUser.isTutore){%>
                <a href="/tutor-materials">Înapoi</a>
                <%} else {%>
                    <a href="/files">Back to all files</a>
                    <%}%>
</body>

</html>